<!-- <script>
    // Set the date we're counting down to
    var countDownDate = new Date("Jan 5, 2024 15:37:25").getTime();
    
    // Update the count down every 1 second
    var x = setInterval(function() {
    
      // Get today's date and time
      var now = new Date().getTime();
    
      // Find the distance between now and the count down date
      var distance = countDownDate - now;
    
      // Time calculations for days, hours, minutes and seconds
      var days = Math.floor(distance / (1000 * 60 * 60 * 24));
      var hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      var seconds = Math.floor((distance % (1000 * 60)) / 1000);
    
      // If the count down is finished, write some text
      if (distance < 0) {
        clearInterval(x);
        document.getElementById("demo").innerHTML = "EXPIRED";
      }
    }, 1000);
    </script>

<div class="countdown">
    <div class="countdown__item">
        <div class="countdown__number">{now}</div>
        <div class="countdown__text">Өдөр</div>
    </div>
    <div class="countdown__item">
        <div class="countdown__number">{hours}</div>
        <div class="countdown__text">Цаг</div>S
    </div>
    <div class="countdown__item">
        <div class="countdown__number">{minutes}</div>
        <div class="countdown__text">Минут</div>
    </div>
    <div class="countdown__item">
        <div class="countdown__number">{seconds}</div>
        <div class="countdown__text">Секунд</div>
    </div>
</div> -->

<p>LESBIAN</p>