<!-- <script>
    import { Mesh, OrbitControls, PerspectiveCamera, AmbientLight } from '@threlte/core'
    import { BoxBufferGeometry, MeshBasicMaterial } from 'three'
  </script>

        <PerspectiveCamera position={{ z: 2 }} lookAt={{y:1, x:0, z:0}} />
        <AmbientLight/>
  <Mesh
    geometry={new BoxBufferGeometry(4, 2, 1)}
    material={new MeshBasicMaterial({ wireframe: false })}
  /> -->